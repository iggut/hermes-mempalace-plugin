# MemPalace v1.3.0 Compatibility Check — 2026-05-21

**Result: PLUGIN FULLY COMPATIBLE — no adjustments needed.**

Checked against upstream at `~/.openclaw/workspace/mempalace/`.

## What's in v1.3.0 (Phases 11–16)

- Phase 16: Integration tests + diagnostics docs
- Phase 15: Audit/improve fact extractor regex quality
- Phase 14: CLI operator workflow and onboarding guide
- Phase 13: MCP parity matrix and two-surface docs
- Phase 12: AAAK dialect compression for digests (default **off**)
- Phase 11: Agent diary read/write on session start/end

## Verified Compatible

| API call | Status | Notes |
|----------|--------|-------|
| `Dialect.compress()` | ✅ | Returns non-empty string; `dialect_compress()` uses correct signature |
| `tool_diary_write` / `tool_diary_read` | ✅ | Signature `agent_name, entry, topic, wing`; lowercase normalization confirmed |
| `mempalace.palace_graph.traverse()` | ✅ | Lazy import in `graph_traverse()` — works even if chromadb unavailable |
| `mempalace.palace_graph.find_tunnels()` | ✅ | Same lazy import pattern |
| `SchemaValidatedFactExtractor` | ✅ | Stop words expanded to 80+, 4+ char single-word minimum |
| `MemPalaceAPI._ensure_imported()` | ✅ | Granular per-module imports; partial failures don't block |
| All `MemPalaceConfig` fields | ✅ | `diary_*`, `aaak_*`, `graph_prefetch_*` all mapped |

## Key Patterns Confirmed

- `_ensure_imported()` uses granular try/except per module — won't fail completely if one submodule (e.g., `palace_graph` which needs chromadb) is unavailable. Only `search_memories` or `get_collection` must succeed to mark imported.
- `graph_traverse()` / `find_tunnels()` are lazy imports inside the method — fail gracefully into `logger.debug()` returning `[]`.
- AAAK is **default off** (`aaak_enabled: bool = False`) — no unexpected compression.
- All 38 contract tests pass, 5 integration tests skipped (require `MEMPALACE_INTEGRATION_TESTS=1`).

## Protocol

1. `python3 -m pytest ~/.hermes/plugins/mempalace/ -q` — confirm 38 tests pass
2. `git -C ~/.hermes/plugins/mempalace log --oneline -10` — check new upstream commits
3. Run smoke test from the SKILL.md
4. If all pass with no mismatches → plugin is current, no action needed