# MemPalace Memory Plugin — config-activated retrieval provider
name: mempalace_memory
version: "1.4.0"
description: "Production-hardened Hermes MemoryProvider for MemPalace: modular refactor (config/api/retrieval/provider), 38 passing contract tests, config-activated bounded recall, diagnostics, safe cache/thread lifecycle, optional ingestion, optional memory mirroring, optional Holographic overlay, KG-assisted recall, and session-end transcript archival."
author: Jupiter
kind: exclusive
requires_env: []
provides_tools: []
provides_hooks: [on_session_start, on_session_end]
provides_memory_provider: mempalace
